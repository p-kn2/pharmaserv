apiVersion: v1
kind: Service
metadata:
  name: pharmaserv-service
  namespace: production
  labels:
    app: pharmaserv-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: pharmaserv-api